# MetaHuman Creator Core Data Status

Status on this PC: installed and verified.

Expected Unreal Engine path:

`C:\Program Files\Epic Games\UE_5.8\Engine\Plugins\MetaHuman\MetaHumanCharacter\Content\Optional`

Required checks passed:

- `Optional` folder exists.
- `Optional\TextureSynthesis` exists.
- `Optional\BodyTextures` exists.
- Texture synthesis `.ar` file exists.
- Body texture `.uasset` files exist.
- Unreal Python verification completed with `0 error(s), 0 warning(s)`.

Installed optional content size:

About 6.17 GB.

Install path in Epic Games Launcher:

`Unreal Engine > Library > UE 5.8 dropdown > Options > MetaHuman Creator Core Data > Apply`

Note:

This content is installed into Unreal Engine itself, not into the project folder. A home PC must install the same option separately.
